using UnityEngine;
using System.Collections;

public class HydrantPlacer : MonoBehaviour {

	// Use this for initialization
	void Start () {
		transform.position = new Vector3 (40f, 4.5f, 0f);
		float timer = 0;

	}
	
	// Update is called once per frame
	void Update () {

	}
}
